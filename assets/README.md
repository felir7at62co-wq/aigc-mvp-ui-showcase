# 资产提示词模板

此目录只保留角色、场景和道具的提示词模板及匹配演示页面。

项目中的资产提取与图片生成由 Web 工作台执行：

- 剧本导入后自动提取角色、场景和道具。
- 图片生成统一调用云映兼容 API 的 `gpt-image-2-official` 模型。
- 认证信息只从环境变量 `YUNYING_API_KEY` 读取。

当前资产生成不再提供独立 CLI，也不依赖外部工作流文件。
